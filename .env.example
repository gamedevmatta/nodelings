# AI Backend — set at least one (server-level keys; users can also bring their own via Settings)
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Optional — for Notion integration buildings
NOTION_TOKEN=

# Session key encryption — generate with: node -e "require('crypto').randomBytes(32).then(b=>console.log(b.toString('hex')))"
# REQUIRED for production; if unset, a dev-only derived key is used (insecure for production)
KEY_ENCRYPTION_KEY=
